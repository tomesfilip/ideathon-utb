export type navLinkType = {
  link: string;
  text: string;
};

export type CardType = {
  selectedTopic?: boolean;
  primaryHeading?: string;
  heading?: string;
  headingTwo?: string;
  text?: string;
  textTwo?: string;
  textExtra?: string;
};
