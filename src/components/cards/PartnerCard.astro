---
interface Props {
  logo: string;
  logoAlt: string;
  text: string;
}
const { logo, logoAlt, text } = Astro.props;
---

<div class="flex flex-col justify-center self-stretch text-center w-full gap-6">
  <img src={logo} alt={logoAlt} class="w-full max-h-8" />
  {text}
</div>
