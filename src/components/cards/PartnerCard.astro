---
interface Props {
  logo: string;
  logoAlt: string;
  text: string;
}
const { logo, logoAlt, text } = Astro.props;
---

<div class="max-w-[400px] text-center w-full space-y-6">
  <img src={logo} alt={logoAlt} class="w-full max-h-8" />
  <p>{text}</p>
</div>
