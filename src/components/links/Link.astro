---
interface Props {
  linkTo: string;
  isBlank?: boolean;
}
const { linkTo = './', isBlank } = Astro.props;
---

<a
  class="cursor-pointer text-white underline text-sm block"
  href={linkTo}
  target={isBlank ? '_blank' : '_self'}
  rel={isBlank ? 'noopener noreferrer' : ''}
>
  <slot />
</a>
